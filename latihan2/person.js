const name = 'jesse';
const age = 420;

export {name,age};
const message = () =>{
    const name = 'H';
    const age = 42;

    return name + ' is ' + age;
} 

export default message;

// export (message);